{% extends "themes/base_admin.html.twig" %}

{% block title %}
    Liste des utilisateurs
{% endblock %}

{% block description %}
    Consultez la liste des vos utilisateurs
{% endblock %}

{% block j_query %}
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js" integrity="sha512-v2CJ7UaYy4JwqLDIrZUI/4hqeoQieOmAZNXBeQyjo21dadnwR+8ZaIJVT8EE2iyI61OV8e6M8PP2/4hpQINQ/g==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
{% endblock %}

{% block data_tables %}
<link href="https://cdn.datatables.net/v/bs5/dt-2.3.7/datatables.min.css" rel="stylesheet" integrity="sha384-MnxpJFHU9dTMYXshJqBDA0d93sG4KKAFEEzynAaDqcPP7BYh11O5HxbJ6iLDWOXS" crossorigin="anonymous">
 
<script src="https://cdn.datatables.net/v/bs5/dt-2.3.7/datatables.min.js" integrity="sha384-7rAjATpiK3MtvKnoDf5WQz4O8W+3kpl4Jm82apgJgNkeUmZ59yjJT6LqlJpYqhVK" crossorigin="anonymous"></script>

<script>
    $(document).ready( function () {
        $('#table').DataTable({
            language: {
                url: 'https://cdn.datatables.net/plug-ins/2.3.7/i18n/fr-FR.json',
            }
        });
    } );
</script>
{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Liste des utilisateurs</h1>

    {% include 'components/_alert.html.twig' %}

    <div class="table-responsive">
        <table id="table" class="table table-striped table-hover table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>Prénom</th>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Statut</th>
                    <th>Rôles</th>
                    <th>Créé le</th>
                    <th>Modifié le</th>
                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{user.firstName}}</td>
                        <td>{{user.lastName}}</td>
                        <td>{{user.email}}</td>
                        <td>
                            {% if user.isVerified %}
                                Vérifié le {{user.verifiedAt|format_date()}}
                            {% else %}
                                Non vérifié
                            {% endif %}    
                        </td>
                        <td>
                            {% for role in user.roles %}
                                <span class="badge text-bg-secondary">{{role}}</span> 
                            {% endfor %}
                        </td>
                        <td>{{user.createdAt|format_date()}}</td>
                        <td>{{user.updatedAt|format_date()}}</td>

                        <td class="align-middle">
                            <div class="d-flex justify-content-center align-items-center gap-1">
                                {% if 'ROLE_SUPER_ADMIN' not in user.roles %}
                                    <a href="{{path('app_admin_user_edit', {'id':user.id})}}" class="btn btn-sm btn-secondary">Modifier</a>
                                    <form action="{{path('app_admin_user_delete', {'id':user.id})}}" method="post">
                                        <input type="hidden" name="csrf_token" value="{{csrf_token('delete-user-'~user.id)}}">
                                        <input type="submit" class="btn btn-sm btn-danger" value="Supprimer" onclick="return confirm('Confirmer la suppression de ce compte?')">
                                    </form>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}